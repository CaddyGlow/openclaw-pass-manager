{
  "name": "openclaw-pass-manager",
  "version": "1.0.2",
  "type": "module",
  "description": "OpenClaw plugin for the standard Unix password manager (pass). Provides agent tools to list, get, save, search passwords and generate OTP codes.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/CaddyGlow/openclaw-pass-manager.git"
  },
  "license": "MIT",
  "keywords": [
    "openclaw",
    "openclaw-plugin",
    "pass",
    "password-store",
    "password-manager",
    "otp",
    "totp",
    "gpg"
  ],
  "files": [
    "src/",
    "!src/**/*.test.ts",
    "openclaw.plugin.json",
    "LICENSE"
  ],
  "openclaw": {
    "extensions": [
      "./src/index.ts"
    ],
    "install": {
      "npmSpec": "openclaw-pass-manager"
    }
  },
  "peerDependencies": {
    "openclaw": ">=2025.0.0"
  },
  "peerDependenciesMeta": {
    "openclaw": {
      "optional": true
    }
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  }
}
